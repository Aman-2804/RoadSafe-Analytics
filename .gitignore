# Python
__pycache__/
*.pyc

# Data artifacts
# - Keep folder structure in git via .gitkeep
# - Allow tracking small helper files in data/raw (README, checksums, samples)
data/**
!data/**/
!data/**/.gitkeep

# Keep raw directory visible/tracked, but do not commit large raw datasets
!data/raw/
!data/raw/**
data/raw/*.csv

# Spark output / parquet
*.parquet
*.snappy.parquet
*.crc

# Local Python packages inside container
.pip-packages/

# Local notes / scratch docs (do not commit)
history.md
QUICK_START.md


